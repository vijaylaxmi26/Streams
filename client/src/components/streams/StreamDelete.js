import React from 'react';
import Model from '../Modal';
import Modal from '../Modal';

const StreamDelete = () => {
    return (
        <div>
           <Model/>
        </div>);
};

export default StreamDelete;